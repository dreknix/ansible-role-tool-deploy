---
sesh_version: 2.26.1

tool_deploy_default_config_sesh:
  sesh:
    action: download_archive
    github:
      repo: joshmedeski/sesh
      file: "v{{ sesh_version | mandatory }}/sesh_Linux_{{ ansible_facts['architecture'] }}.tar.gz"
    version:
      args: --version
      match: "sesh version {{ sesh_version | mandatory }}"
    man_pages:
      src:
        - share/man/man1/sesh.1
    bash_completion:
      generate_args: completion bash
    zsh_completion:
      generate_args: completion zsh
...
