---

ty_version: 0.0.1-alpha.27

tool_deploy_default_config_ty:
  ty:
    action: download_archive
    github:
      repo: astral-sh/ty
      file: "{{ ty_version | mandatory }}/ty-x86_64-unknown-linux-gnu.tar.gz"
    version:
      args: --version
      match: "ty {{ ty_version | mandatory }}"

...
